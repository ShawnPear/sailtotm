<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mapper.OneBoundApiTaobaoProductMapper">
    <insert id="insert">
        insert into OneBoundApiTaobaoProduct (created_date, detail_url, num_iid, pic_url, price, promotion_price, sales,
                                              saller_nick, title, original_json, q)
        VALUES (#{created_date},#{detailUrl},#{numIid},#{picUrl},#{price},#{promotionPrice},#{sales},#{sallerNick},#{title},#{originalJson},#{q})
    </insert>
</mapper>